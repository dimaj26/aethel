---
name: project-overview
description: Stack, directory layout, and how the major pieces fit together.
---

# Project Overview

This is the first topic file of the `knowledge/` tree. The index ([CONTEXT.md](../CONTEXT.md))
links here; replace this content with your project's actual overview.

## Stack
- Languages / runtimes / frameworks this project is built on.

## Directory Layout
- The top-level directories and what each is responsible for.

## How It Fits Together
- The request/data flow across the main layers or subsystems.

## Topic Files
Add one `knowledge/<topic>.md` per layer, subsystem, or bounded context (one H1, ~50–200
lines, `name` + `description` frontmatter), and link it from the index. Record notable
design decisions as ADRs under [decisions/](decisions/0001-record-architecture-decisions.md).
